diff -ruN a/node_modules/@sbaiahmed1/react-native-blur/ios/Helpers/ReactNativeProgressiveBlurViewHelper.swift b/node_modules/@sbaiahmed1/react-native-blur/ios/Helpers/ReactNativeProgressiveBlurViewHelper.swift
--- a/node_modules/@sbaiahmed1/react-native-blur/ios/Helpers/ReactNativeProgressiveBlurViewHelper.swift	2026-03-06 18:10:31
+++ b/node_modules/@sbaiahmed1/react-native-blur/ios/Helpers/ReactNativeProgressiveBlurViewHelper.swift	2026-03-06 18:10:31
@@ -27,6 +27,66 @@
     blurView.startOffset = startOffset
   }
 
+  /// Updates whether the progressive blur view uses a radial mask.
+  @objc public static func updateProgressiveBlurView(_ blurView: ProgressiveBlurView, withRadial radial: Bool) {
+    blurView.radial = radial
+  }
+
+  /// Updates the radial mask center x fraction.
+  @objc public static func updateProgressiveBlurView(_ blurView: ProgressiveBlurView, withRadialCenterX radialCenterX: Double) {
+    blurView.radialCenterX = radialCenterX
+  }
+
+  /// Updates the radial mask center y fraction.
+  @objc public static func updateProgressiveBlurView(_ blurView: ProgressiveBlurView, withRadialCenterY radialCenterY: Double) {
+    blurView.radialCenterY = radialCenterY
+  }
+
+  /// Updates the radial mask clear radius in points.
+  @objc public static func updateProgressiveBlurView(_ blurView: ProgressiveBlurView, withRadialClearRadius radialClearRadius: Double) {
+    blurView.radialClearRadius = radialClearRadius
+  }
+
+  /// Updates the radial mask feather width in points.
+  @objc public static func updateProgressiveBlurView(_ blurView: ProgressiveBlurView, withRadialFeather radialFeather: Double) {
+    blurView.radialFeather = radialFeather
+  }
+
+  /// Updates the reveal trigger value.
+  @objc public static func updateProgressiveBlurView(_ blurView: ProgressiveBlurView, withRevealTrigger revealTrigger: Double) {
+    blurView.revealTrigger = revealTrigger
+  }
+
+  /// Updates the native reveal animation duration.
+  @objc public static func updateProgressiveBlurView(_ blurView: ProgressiveBlurView, withAnimationDuration animationDuration: Double) {
+    blurView.animationDuration = animationDuration
+  }
+
+  /// Updates the native reveal animation delay.
+  @objc public static func updateProgressiveBlurView(_ blurView: ProgressiveBlurView, withAnimationDelay animationDelay: Double) {
+    blurView.animationDelay = animationDelay
+  }
+
+  /// Updates the reveal starting radius.
+  @objc public static func updateProgressiveBlurView(_ blurView: ProgressiveBlurView, withStartRadius startRadius: Double) {
+    blurView.startRadius = startRadius
+  }
+
+  /// Updates the reveal ending radius.
+  @objc public static func updateProgressiveBlurView(_ blurView: ProgressiveBlurView, withEndRadius endRadius: Double) {
+    blurView.endRadius = endRadius
+  }
+
+  /// Updates the reveal starting feather width.
+  @objc public static func updateProgressiveBlurView(_ blurView: ProgressiveBlurView, withFeatherStart featherStart: Double) {
+    blurView.featherStart = featherStart
+  }
+
+  /// Updates the reveal ending feather width.
+  @objc public static func updateProgressiveBlurView(_ blurView: ProgressiveBlurView, withFeatherEnd featherEnd: Double) {
+    blurView.featherEnd = featherEnd
+  }
+
   /// Updates the progressive blur view with a new blur type.
   @objc public static func updateProgressiveBlurView(_ blurView: ProgressiveBlurView, withBlurType blurType: String) {
     blurView.blurTypeString = blurType
diff -ruN a/node_modules/@sbaiahmed1/react-native-blur/ios/ReactNativeProgressiveBlurView.mm b/node_modules/@sbaiahmed1/react-native-blur/ios/ReactNativeProgressiveBlurView.mm
--- a/node_modules/@sbaiahmed1/react-native-blur/ios/ReactNativeProgressiveBlurView.mm	2026-03-06 18:10:31
+++ b/node_modules/@sbaiahmed1/react-native-blur/ios/ReactNativeProgressiveBlurView.mm	2026-03-06 18:10:31
@@ -136,6 +136,18 @@
     }
 
     [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withStartOffset:pbvProps.startOffset];
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withRadial:pbvProps.radial];
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withRadialCenterX:pbvProps.radialCenterX];
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withRadialCenterY:pbvProps.radialCenterY];
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withRadialClearRadius:pbvProps.radialClearRadius];
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withRadialFeather:pbvProps.radialFeather];
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withAnimationDuration:pbvProps.animationDuration];
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withAnimationDelay:pbvProps.animationDelay];
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withStartRadius:pbvProps.startRadius];
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withEndRadius:pbvProps.endRadius];
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withFeatherStart:pbvProps.featherStart];
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withFeatherEnd:pbvProps.featherEnd];
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withRevealTrigger:pbvProps.revealTrigger];
 
     if (!pbvProps.reducedTransparencyFallbackColor.empty()) {
       NSString *fallbackColorString = [[NSString alloc] initWithUTF8String:pbvProps.reducedTransparencyFallbackColor.c_str()];
@@ -169,6 +181,54 @@
 
   if (oldViewProps.startOffset != newViewProps.startOffset) {
     [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withStartOffset:newViewProps.startOffset];
+  }
+
+  if (oldViewProps.radial != newViewProps.radial) {
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withRadial:newViewProps.radial];
+  }
+
+  if (oldViewProps.radialCenterX != newViewProps.radialCenterX) {
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withRadialCenterX:newViewProps.radialCenterX];
+  }
+
+  if (oldViewProps.radialCenterY != newViewProps.radialCenterY) {
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withRadialCenterY:newViewProps.radialCenterY];
+  }
+
+  if (oldViewProps.radialClearRadius != newViewProps.radialClearRadius) {
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withRadialClearRadius:newViewProps.radialClearRadius];
+  }
+
+  if (oldViewProps.radialFeather != newViewProps.radialFeather) {
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withRadialFeather:newViewProps.radialFeather];
+  }
+
+  if (oldViewProps.animationDuration != newViewProps.animationDuration) {
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withAnimationDuration:newViewProps.animationDuration];
+  }
+
+  if (oldViewProps.animationDelay != newViewProps.animationDelay) {
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withAnimationDelay:newViewProps.animationDelay];
+  }
+
+  if (oldViewProps.startRadius != newViewProps.startRadius) {
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withStartRadius:newViewProps.startRadius];
+  }
+
+  if (oldViewProps.endRadius != newViewProps.endRadius) {
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withEndRadius:newViewProps.endRadius];
+  }
+
+  if (oldViewProps.featherStart != newViewProps.featherStart) {
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withFeatherStart:newViewProps.featherStart];
+  }
+
+  if (oldViewProps.featherEnd != newViewProps.featherEnd) {
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withFeatherEnd:newViewProps.featherEnd];
+  }
+
+  if (oldViewProps.revealTrigger != newViewProps.revealTrigger) {
+    [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:_progressiveBlurView withRevealTrigger:newViewProps.revealTrigger];
   }
 
   if (oldViewProps.reducedTransparencyFallbackColor != newViewProps.reducedTransparencyFallbackColor) {
diff -ruN a/node_modules/@sbaiahmed1/react-native-blur/ios/ReactNativeProgressiveBlurViewManager.mm b/node_modules/@sbaiahmed1/react-native-blur/ios/ReactNativeProgressiveBlurViewManager.mm
--- a/node_modules/@sbaiahmed1/react-native-blur/ios/ReactNativeProgressiveBlurViewManager.mm	2026-03-06 18:10:31
+++ b/node_modules/@sbaiahmed1/react-native-blur/ios/ReactNativeProgressiveBlurViewManager.mm	2026-03-06 18:10:31
@@ -48,6 +48,78 @@
   [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:view withStartOffset:offset];
 }
 
+RCT_CUSTOM_VIEW_PROPERTY(radial, BOOL, ProgressiveBlurView)
+{
+  BOOL radial = json ? [RCTConvert BOOL:json] : NO;
+  [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:view withRadial:radial];
+}
+
+RCT_CUSTOM_VIEW_PROPERTY(radialCenterX, NSNumber, ProgressiveBlurView)
+{
+  double value = json ? [RCTConvert double:json] : 0.5;
+  [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:view withRadialCenterX:value];
+}
+
+RCT_CUSTOM_VIEW_PROPERTY(radialCenterY, NSNumber, ProgressiveBlurView)
+{
+  double value = json ? [RCTConvert double:json] : 0.5;
+  [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:view withRadialCenterY:value];
+}
+
+RCT_CUSTOM_VIEW_PROPERTY(radialClearRadius, NSNumber, ProgressiveBlurView)
+{
+  double value = json ? [RCTConvert double:json] : 0.0;
+  [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:view withRadialClearRadius:value];
+}
+
+RCT_CUSTOM_VIEW_PROPERTY(radialFeather, NSNumber, ProgressiveBlurView)
+{
+  double value = json ? [RCTConvert double:json] : 120.0;
+  [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:view withRadialFeather:value];
+}
+
+RCT_CUSTOM_VIEW_PROPERTY(revealTrigger, NSNumber, ProgressiveBlurView)
+{
+  double value = json ? [RCTConvert double:json] : 0.0;
+  [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:view withRevealTrigger:value];
+}
+
+RCT_CUSTOM_VIEW_PROPERTY(animationDuration, NSNumber, ProgressiveBlurView)
+{
+  double value = json ? [RCTConvert double:json] : 20000.0;
+  [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:view withAnimationDuration:value];
+}
+
+RCT_CUSTOM_VIEW_PROPERTY(animationDelay, NSNumber, ProgressiveBlurView)
+{
+  double value = json ? [RCTConvert double:json] : 200.0;
+  [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:view withAnimationDelay:value];
+}
+
+RCT_CUSTOM_VIEW_PROPERTY(startRadius, NSNumber, ProgressiveBlurView)
+{
+  double value = json ? [RCTConvert double:json] : 0.0;
+  [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:view withStartRadius:value];
+}
+
+RCT_CUSTOM_VIEW_PROPERTY(endRadius, NSNumber, ProgressiveBlurView)
+{
+  double value = json ? [RCTConvert double:json] : 1000.0;
+  [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:view withEndRadius:value];
+}
+
+RCT_CUSTOM_VIEW_PROPERTY(featherStart, NSNumber, ProgressiveBlurView)
+{
+  double value = json ? [RCTConvert double:json] : 120.0;
+  [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:view withFeatherStart:value];
+}
+
+RCT_CUSTOM_VIEW_PROPERTY(featherEnd, NSNumber, ProgressiveBlurView)
+{
+  double value = json ? [RCTConvert double:json] : 160.0;
+  [ReactNativeProgressiveBlurViewHelper updateProgressiveBlurView:view withFeatherEnd:value];
+}
+
 RCT_CUSTOM_VIEW_PROPERTY(reducedTransparencyFallbackColor, NSString, ProgressiveBlurView)
 {
   NSString *colorString = json ? [RCTConvert NSString:json] : @"#FFFFFF";
diff -ruN a/node_modules/@sbaiahmed1/react-native-blur/ios/Views/ProgressiveBlurView.swift b/node_modules/@sbaiahmed1/react-native-blur/ios/Views/ProgressiveBlurView.swift
--- a/node_modules/@sbaiahmed1/react-native-blur/ios/Views/ProgressiveBlurView.swift	2026-03-06 18:10:31
+++ b/node_modules/@sbaiahmed1/react-native-blur/ios/Views/ProgressiveBlurView.swift	2026-03-06 18:10:31
@@ -3,10 +3,17 @@
 
 import SwiftUI
 import UIKit
+import CoreImage
+import QuartzCore
 
 @objc public class ProgressiveBlurView: UIView {
 
   private var variableBlurView: VariableBlurView?
+  private var snapshotImageView: UIImageView?
+  private let snapshotMaskLayer = CAGradientLayer()
+  private let ciContext = CIContext()
+  private var snapshotCleanupWorkItem: DispatchWorkItem?
+  private var snapshotBoundsSize: CGSize = .zero
 
   @objc public var blurAmount: Double = 20.0 {
     didSet {
@@ -26,6 +33,56 @@
     }
   }
 
+  @objc public var radial: Bool = false {
+    didSet {
+      updateBlur()
+    }
+  }
+
+  @objc public var radialCenterX: Double = 0.5 {
+    didSet {
+      updateBlur()
+    }
+  }
+
+  @objc public var radialCenterY: Double = 0.5 {
+    didSet {
+      updateBlur()
+    }
+  }
+
+  @objc public var radialClearRadius: Double = 0.0 {
+    didSet {
+      updateBlur()
+    }
+  }
+
+  @objc public var radialFeather: Double = 120.0 {
+    didSet {
+      updateBlur()
+    }
+  }
+
+  @objc public var revealTrigger: Double = 0.0 {
+    didSet {
+      if revealTrigger != oldValue {
+        startRevealAnimation()
+      }
+    }
+  }
+
+  @objc public var animationDuration: Double = 20000.0
+
+  @objc public var animationDelay: Double = 200.0
+
+  @objc public var startRadius: Double = 0.0
+
+  @objc public var endRadius: Double = 1000.0
+
+  @objc public var featherStart: Double = 120.0
+
+  @objc public var featherEnd: Double = 160.0
+
   @objc public var blurTypeString: String = "regular" {
     didSet {
       updateBlur()
@@ -51,6 +108,7 @@
   private func setupView() {
     // Remove old view if exists
     variableBlurView?.removeFromSuperview()
+    snapshotImageView?.removeFromSuperview()
 
     let blurStyle = blurStyleFromString(blurTypeString)
     let blurDirection = VariableBlurDirection(fromString: direction)
@@ -73,6 +131,7 @@
     ])
 
     self.variableBlurView = variableBlur
+    updateBlur()
 
     // Handle reduced transparency
     if UIAccessibility.isReduceTransparencyEnabled {
@@ -90,6 +149,15 @@
       return
     }
 
+    if radial {
+      variableBlurView.isHidden = true
+      backgroundColor = .clear
+      ensureSnapshotImageView()
+      refreshSnapshotIfNeeded(force: snapshotImageView?.image == nil)
+      updateSnapshotMask()
+      return
+    }
+
     let blurStyle = blurStyleFromString(blurTypeString)
     let blurDirection = VariableBlurDirection(fromString: direction)
 
@@ -97,6 +165,11 @@
       maxBlurRadius: CGFloat(blurAmount),
       direction: blurDirection,
       startOffset: CGFloat(startOffset),
+      radial: radial,
+      radialCenterX: CGFloat(radialCenterX),
+      radialCenterY: CGFloat(radialCenterY),
+      radialClearRadius: CGFloat(radialClearRadius),
+      radialFeather: CGFloat(radialFeather),
       blurStyle: blurStyle
     )
 
@@ -110,15 +183,299 @@
     }
   }
 
+  private func startRevealAnimation() {
+    guard let variableBlurView = variableBlurView else {
+      return
+    }
+
+    if radial {
+      refreshSnapshotIfNeeded(force: true)
+      startSnapshotRevealAnimation()
+      return
+    }
+
+    updateBlur()
+    variableBlurView.startRevealAnimation(
+      durationMs: CGFloat(animationDuration),
+      delayMs: CGFloat(animationDelay),
+      fromRadius: CGFloat(startRadius),
+      toRadius: CGFloat(endRadius),
+      fromFeather: CGFloat(featherStart),
+      toFeather: CGFloat(featherEnd)
+    )
+  }
+
   public override func didMoveToWindow() {
     super.didMoveToWindow()
     if window != nil {
       updateBlur()
+      if radial {
+        refreshSnapshotIfNeeded(force: true)
+      }
     }
   }
 
+  public override func layoutSubviews() {
+    super.layoutSubviews()
+
+    if snapshotBoundsSize != bounds.size {
+      snapshotBoundsSize = bounds.size
+      if radial {
+        refreshSnapshotIfNeeded(force: true)
+        updateSnapshotMask()
+      }
+    }
+  }
+
+  private func ensureSnapshotImageView() {
+    if let snapshotImageView {
+      if snapshotImageView.superview == nil {
+        addSubview(snapshotImageView)
+      }
+      snapshotImageView.frame = bounds
+      bringSubviewToFront(snapshotImageView)
+      return
+    }
+
+    let imageView = UIImageView(frame: bounds)
+    imageView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
+    imageView.contentMode = .scaleToFill
+    imageView.clipsToBounds = true
+    addSubview(imageView)
+    bringSubviewToFront(imageView)
+    snapshotImageView = imageView
+  }
+
+  private func refreshSnapshotIfNeeded(force: Bool) {
+    guard radial else {
+      return
+    }
+
+    ensureSnapshotImageView()
+
+    if !force,
+       snapshotImageView?.image != nil,
+       snapshotBoundsSize == bounds.size {
+      return
+    }
+
+    snapshotBoundsSize = bounds.size
+    snapshotCleanupWorkItem?.cancel()
+
+    guard let snapshotImageView,
+          let blurredSnapshot = captureBlurredSnapshot() else {
+      return
+    }
+
+    snapshotImageView.image = blurredSnapshot
+    snapshotImageView.isHidden = false
+  }
+
+  private func captureBlurredSnapshot() -> UIImage? {
+    guard let window,
+          bounds.width > 0,
+          bounds.height > 0 else {
+      return nil
+    }
+
+    let captureRect = convert(bounds, to: window)
+    let rendererFormat = UIGraphicsImageRendererFormat.default()
+    rendererFormat.scale = window.screen.scale
+    rendererFormat.opaque = false
+
+    let wasHidden = isHidden
+    isHidden = true
+    defer {
+      isHidden = wasHidden
+    }
+
+    let renderer = UIGraphicsImageRenderer(size: captureRect.size, format: rendererFormat)
+    let snapshot = renderer.image { context in
+      context.cgContext.translateBy(x: -captureRect.origin.x, y: -captureRect.origin.y)
+      window.drawHierarchy(in: window.bounds, afterScreenUpdates: false)
+    }
+
+    return makeBlurredSnapshot(from: snapshot)
+  }
+
+  private func makeBlurredSnapshot(from image: UIImage) -> UIImage? {
+    guard let inputImage = CIImage(image: image) else {
+      return image
+    }
+
+    let clampedImage = inputImage.clampedToExtent()
+    let blurFilter = CIFilter.gaussianBlur()
+    blurFilter.inputImage = clampedImage
+    blurFilter.radius = Float(max(blurAmount, 1))
+
+    let saturationFilter = CIFilter.colorControls()
+    saturationFilter.inputImage = blurFilter.outputImage?.cropped(to: inputImage.extent)
+    saturationFilter.saturation = 1.8
+    saturationFilter.brightness = 0
+    saturationFilter.contrast = 1.05
+
+    guard let outputImage = saturationFilter.outputImage?.cropped(to: inputImage.extent),
+          let cgImage = ciContext.createCGImage(outputImage, from: inputImage.extent) else {
+      return image
+    }
+
+    return UIImage(cgImage: cgImage, scale: image.scale, orientation: image.imageOrientation)
+  }
+
+  private func updateSnapshotMask() {
+    guard radial,
+          let snapshotImageView,
+          bounds.width > 0,
+          bounds.height > 0 else {
+      snapshotImageView?.layer.mask = nil
+      return
+    }
+
+    let envelopeRadius = resolvedMaskEnvelopeRadius(
+      clearRadius: CGFloat(radialClearRadius),
+      feather: CGFloat(radialFeather)
+    )
+    configureSnapshotMaskLayer(envelopeRadius: envelopeRadius)
+
+    CATransaction.begin()
+    CATransaction.setDisableActions(true)
+    snapshotMaskLayer.colors = maskColors()
+    snapshotMaskLayer.locations = maskLocations(
+      clearRadius: CGFloat(radialClearRadius),
+      feather: CGFloat(radialFeather),
+      envelopeRadius: envelopeRadius
+    )
+    snapshotImageView.layer.mask = snapshotMaskLayer
+    CATransaction.commit()
+  }
+
+  private func startSnapshotRevealAnimation() {
+    guard let snapshotImageView else {
+      return
+    }
+
+    let fromRadius = CGFloat(startRadius)
+    let toRadius = CGFloat(endRadius)
+    let fromFeatherValue = CGFloat(featherStart)
+    let toFeatherValue = CGFloat(featherEnd)
+    let envelopeRadius = resolvedMaskEnvelopeRadius(
+      clearRadius: toRadius,
+      feather: toFeatherValue
+    )
+
+    configureSnapshotMaskLayer(envelopeRadius: envelopeRadius)
+
+    let startLocations = maskLocations(
+      clearRadius: fromRadius,
+      feather: fromFeatherValue,
+      envelopeRadius: envelopeRadius
+    )
+    let endLocations = maskLocations(
+      clearRadius: toRadius,
+      feather: toFeatherValue,
+      envelopeRadius: envelopeRadius
+    )
+
+    snapshotCleanupWorkItem?.cancel()
+
+    CATransaction.begin()
+    CATransaction.setDisableActions(true)
+    snapshotMaskLayer.colors = maskColors()
+    snapshotMaskLayer.locations = endLocations
+    snapshotImageView.layer.mask = snapshotMaskLayer
+    CATransaction.commit()
+
+    let revealAnimation = CABasicAnimation(keyPath: "locations")
+    revealAnimation.fromValue = startLocations
+    revealAnimation.toValue = endLocations
+    revealAnimation.beginTime = CACurrentMediaTime() + CFTimeInterval(max(animationDelay, 0) / 1000)
+    revealAnimation.duration = CFTimeInterval(max(animationDuration, 1) / 1000)
+    revealAnimation.timingFunction = CAMediaTimingFunction(
+      controlPoints: 0.05,
+      0.9,
+      0.2,
+      1
+    )
+    revealAnimation.fillMode = .both
+    revealAnimation.isRemovedOnCompletion = true
+    snapshotMaskLayer.add(revealAnimation, forKey: "snapshotRevealLocations")
+
+    let cleanupWorkItem = DispatchWorkItem { [weak self] in
+      self?.snapshotMaskLayer.removeAnimation(forKey: "snapshotRevealLocations")
+      self?.snapshotImageView?.layer.mask = nil
+      self?.snapshotImageView?.image = nil
+      self?.snapshotImageView?.isHidden = true
+    }
+    snapshotCleanupWorkItem = cleanupWorkItem
+    DispatchQueue.main.asyncAfter(
+      deadline: .now() + max(animationDelay + animationDuration, 1) / 1000,
+      execute: cleanupWorkItem
+    )
+  }
+
+  private func configureSnapshotMaskLayer(envelopeRadius: CGFloat) {
+    let safeEnvelopeRadius = max(envelopeRadius, 1)
+    let diameter = safeEnvelopeRadius * 2
+    let originPoint = CGPoint(
+      x: min(max(CGFloat(radialCenterX), 0), 1) * bounds.width,
+      y: min(max(CGFloat(radialCenterY), 0), 1) * bounds.height
+    )
+
+    CATransaction.begin()
+    CATransaction.setDisableActions(true)
+    snapshotMaskLayer.type = .radial
+    snapshotMaskLayer.startPoint = CGPoint(x: 0.5, y: 0.5)
+    snapshotMaskLayer.endPoint = CGPoint(x: 1, y: 0.5)
+    snapshotMaskLayer.bounds = CGRect(x: 0, y: 0, width: diameter, height: diameter)
+    snapshotMaskLayer.position = originPoint
+    CATransaction.commit()
+  }
+
+  private func maskColors() -> [CGColor] {
+    [
+      UIColor.clear.cgColor,
+      UIColor.clear.cgColor,
+      UIColor.black.cgColor
+    ]
+  }
+
+  private func maskLocations(
+    clearRadius: CGFloat,
+    feather: CGFloat,
+    envelopeRadius: CGFloat
+  ) -> [NSNumber] {
+    let safeEnvelopeRadius = max(envelopeRadius, 1)
+    let safeClearRadius = max(clearRadius, 0)
+    let safeFeather = max(feather, 1)
+    let clearStop = min(max(safeClearRadius / safeEnvelopeRadius, 0), 1)
+    let opaqueStop = min(max((safeClearRadius + safeFeather) / safeEnvelopeRadius, clearStop), 1)
+
+    return [
+      0,
+      NSNumber(value: Double(clearStop)),
+      NSNumber(value: Double(opaqueStop))
+    ]
+  }
+
+  private func resolvedMaskEnvelopeRadius(clearRadius: CGFloat, feather: CGFloat) -> CGFloat {
+    let centerX = min(max(CGFloat(radialCenterX), 0), 1) * bounds.width
+    let centerY = min(max(CGFloat(radialCenterY), 0), 1) * bounds.height
+    let farthestCorner = max(
+      hypot(centerX, centerY),
+      hypot(bounds.width - centerX, centerY),
+      hypot(centerX, bounds.height - centerY),
+      hypot(bounds.width - centerX, bounds.height - centerY)
+    )
+
+    return max(clearRadius + feather, farthestCorner + max(feather, 1), 1)
+  }
+
   deinit {
+    snapshotCleanupWorkItem?.cancel()
+    snapshotMaskLayer.removeAnimation(forKey: "snapshotRevealLocations")
     variableBlurView?.removeFromSuperview()
+    snapshotImageView?.removeFromSuperview()
     variableBlurView = nil
+    snapshotImageView = nil
   }
 }
diff -ruN a/node_modules/@sbaiahmed1/react-native-blur/ios/Views/VariableBlurView.swift b/node_modules/@sbaiahmed1/react-native-blur/ios/Views/VariableBlurView.swift
--- a/node_modules/@sbaiahmed1/react-native-blur/ios/Views/VariableBlurView.swift	2026-03-06 18:10:31
+++ b/node_modules/@sbaiahmed1/react-native-blur/ios/Views/VariableBlurView.swift	2026-03-06 18:10:31
@@ -28,6 +28,32 @@
   private var maxBlurRadius: CGFloat = 20
   private var direction: VariableBlurDirection = .blurredTopClearBottom
   private var startOffset: CGFloat = 0
+  private var radial: Bool = false
+  private var radialCenterX: CGFloat = 0.5
+  private var radialCenterY: CGFloat = 0.5
+  private var radialClearRadius: CGFloat = 0
+  private var radialFeather: CGFloat = 120
+  private var currentBlurStyle: UIBlurEffect.Style = .regular
+  private var appliedBlurStyle: UIBlurEffect.Style?
+  private var variableBlurFilter: NSObject?
+  private weak var backdropLayerRef: CALayer?
+  private let radialMaskLayer = CAGradientLayer()
+  private var displayLink: CADisplayLink?
+  private var animationStartTime: CFTimeInterval = 0
+  private var animationDelaySeconds: CFTimeInterval = 0
+  private var animationDurationSeconds: CFTimeInterval = 0
+  private var animationFromRadius: CGFloat = 0
+  private var animationToRadius: CGFloat = 0
+  private var animationFromFeather: CGFloat = 120
+  private var animationToFeather: CGFloat = 120
+  private var currentAnimatedClearRadius: CGFloat = 0
+  private var currentAnimatedFeather: CGFloat = 120
+  private var cachedMaskCanvasSize: CGSize = .zero
+  private var cachedMaskContext: CGContext?
+  private var cachedRadialGradient: CGGradient?
+  private var lastAppliedBlurRadius: CGFloat?
+  private var lastRenderedClearRadius: CGFloat?
+  private var lastRenderedFeather: CGFloat?
 
   public init(
     maxBlurRadius: CGFloat = 20,
@@ -38,6 +64,9 @@
     self.maxBlurRadius = maxBlurRadius
     self.direction = direction
     self.startOffset = startOffset
+    self.currentBlurStyle = blurStyle
+    self.currentAnimatedClearRadius = 0
+    self.currentAnimatedFeather = 120
 
     super.init(effect: UIBlurEffect(style: blurStyle))
 
@@ -53,66 +82,173 @@
     maxBlurRadius: CGFloat,
     direction: VariableBlurDirection,
     startOffset: CGFloat,
+    radial: Bool,
+    radialCenterX: CGFloat,
+    radialCenterY: CGFloat,
+    radialClearRadius: CGFloat,
+    radialFeather: CGFloat,
     blurStyle: UIBlurEffect.Style = .regular
   ) {
     self.maxBlurRadius = maxBlurRadius
     self.direction = direction
     self.startOffset = startOffset
-    self.effect = UIBlurEffect(style: blurStyle)
+    self.radial = radial
+    self.radialCenterX = radialCenterX
+    self.radialCenterY = radialCenterY
+    self.radialClearRadius = radialClearRadius
+    self.radialFeather = radialFeather
+    self.currentBlurStyle = blurStyle
 
-    setupVariableBlur()
+    if displayLink == nil {
+      currentAnimatedClearRadius = radialClearRadius
+      currentAnimatedFeather = radialFeather
+    }
+
+    applyCurrentConfiguration()
   }
 
   private func setupVariableBlur() {
-    // Creates filter via runtime reflection
-    // This uses a private Core Animation filter called "variableBlur"
-    let clsName = String("retliFAC".reversed()) // CAFilter
-    guard let Cls = NSClassFromString(clsName) as? NSObject.Type else {
-      print("[VariableBlur] Error: Can't find filter class")
+    applyCurrentConfiguration()
+  }
+
+  private func applyCurrentConfiguration() {
+    if radial {
+      applyRadialMaskConfiguration()
       return
     }
 
-    let selName = String(":epyThtiWretlif".reversed()) // filterWithType:
-    guard let variableBlur = Cls.self.perform(
+    if layer.mask != nil {
+      layer.mask = nil
+    }
+
+    guard let variableBlur = ensureBaseConfiguration() else {
+      return
+    }
+
+    updateFilterMaskAndRadius(variableBlur, forceMaskRefresh: true)
+  }
+
+  private func ensureVariableBlurFilter() -> NSObject? {
+    if let variableBlurFilter {
+      return variableBlurFilter
+    }
+
+    let clsName = String("retliFAC".reversed())
+    guard let cls = NSClassFromString(clsName) as? NSObject.Type else {
+      print("[VariableBlur] Error: Can't find filter class")
+      return nil
+    }
+
+    let selName = String(":epyThtiWretlif".reversed())
+    guard let variableBlur = cls.perform(
       NSSelectorFromString(selName),
       with: "variableBlur"
     )?.takeUnretainedValue() as? NSObject else {
       print("[VariableBlur] Error: Can't create variableBlur filter")
-      return
+      return nil
     }
 
-    let gradientImage = makeGradientImage(
-      startOffset: startOffset,
-      direction: direction
-    )
+    variableBlurFilter = variableBlur
+    return variableBlur
+  }
 
-    variableBlur.setValue(maxBlurRadius, forKey: "inputRadius")
-    variableBlur.setValue(gradientImage, forKey: "inputMaskImage")
-    variableBlur.setValue(true, forKey: "inputNormalizeEdges")
+  private func ensureBackdropLayer() -> CALayer? {
+    if let backdropLayerRef {
+      return backdropLayerRef
+    }
 
     let backdropLayer = subviews.first?.layer
-    backdropLayer?.filters = [variableBlur]
+    backdropLayerRef = backdropLayer
+    return backdropLayer
+  }
 
-    // Hide the default visual effect view overlays
-    for subview in subviews.dropFirst() {
-      subview.alpha = 0
+  public func startRevealAnimation(
+    durationMs: CGFloat,
+    delayMs: CGFloat,
+    fromRadius: CGFloat,
+    toRadius: CGFloat,
+    fromFeather: CGFloat,
+    toFeather: CGFloat
+  ) {
+    stopRevealAnimation()
+
+    if radial {
+      startRadialMaskRevealAnimation(
+        durationMs: durationMs,
+        delayMs: delayMs,
+        fromRadius: fromRadius,
+        toRadius: toRadius,
+        fromFeather: fromFeather,
+        toFeather: toFeather
+      )
+      return
     }
+
+    animationStartTime = CACurrentMediaTime()
+    animationDelaySeconds = CFTimeInterval(max(delayMs, 0) / 1000)
+    animationDurationSeconds = CFTimeInterval(max(durationMs, 1) / 1000)
+    animationFromRadius = fromRadius
+    animationToRadius = toRadius
+    animationFromFeather = fromFeather
+    animationToFeather = toFeather
+    currentAnimatedClearRadius = fromRadius
+    currentAnimatedFeather = fromFeather
+    applyCurrentConfiguration()
+
+    let displayLink = CADisplayLink(target: self, selector: #selector(handleRevealDisplayLink))
+    if #available(iOS 15.0, *) {
+      displayLink.preferredFrameRateRange = CAFrameRateRange(minimum: 80, maximum: 120, preferred: 120)
+    } else {
+      displayLink.preferredFramesPerSecond = 120
+    }
+    displayLink.add(to: .main, forMode: .common)
+    self.displayLink = displayLink
   }
 
-  open override func didMoveToWindow() {
-    super.didMoveToWindow()
-    guard let window, let backdropLayer = subviews.first?.layer else { return }
+  public func stopRevealAnimation() {
+    displayLink?.invalidate()
+    displayLink = nil
+    radialMaskLayer.removeAnimation(forKey: "radialRevealLocations")
+  }
 
-    let newScale = window.traitCollection.displayScale
-    let currentScale = backdropLayer.value(forKey: "scale") as? CGFloat
+  @objc private func handleRevealDisplayLink() {
+    let elapsed = CACurrentMediaTime() - animationStartTime - animationDelaySeconds
+    if elapsed < 0 {
+      return
+    }
 
-    // Only update scale if it actually changed to prevent unnecessary
-    // recalculations during navigation gestures
-    if currentScale != newScale {
-      backdropLayer.setValue(newScale, forKey: "scale")
+    let rawProgress = min(max(CGFloat(elapsed / animationDurationSeconds), 0), 1)
+    let easedProgress = memoriumRadiusEase(rawProgress)
+    currentAnimatedClearRadius = interpolate(
+      from: animationFromRadius,
+      to: animationToRadius,
+      progress: easedProgress
+    )
+    currentAnimatedFeather = interpolate(
+      from: animationFromFeather,
+      to: animationToFeather,
+      progress: easedProgress
+    )
+    if let variableBlur = ensureBaseConfiguration() {
+      updateFilterMaskAndRadius(variableBlur, forceMaskRefresh: false)
     }
+
+    if rawProgress >= 1 {
+      stopRevealAnimation()
+      radialClearRadius = animationToRadius
+      radialFeather = animationToFeather
+      currentAnimatedClearRadius = animationToRadius
+      currentAnimatedFeather = animationToFeather
+      applyCurrentConfiguration()
+    }
   }
 
+  open override func didMoveToWindow() {
+    super.didMoveToWindow()
+    guard let backdropLayer = ensureBackdropLayer() else { return }
+    updateBackdropScaleIfNeeded(backdropLayer)
+  }
+
   open override func traitCollectionDidChange(
     _ previousTraitCollection: UITraitCollection?
   ) {
@@ -120,16 +256,26 @@
     // Re-setup blur if needed when trait collection changes
     if let previousTraitCollection = previousTraitCollection,
        traitCollection.userInterfaceStyle != previousTraitCollection.userInterfaceStyle {
-      setupVariableBlur()
+      applyCurrentConfiguration()
     }
   }
 
+  open override func layoutSubviews() {
+    super.layoutSubviews()
+    invalidateMaskResources()
+    applyCurrentConfiguration()
+  }
+
   private func makeGradientImage(
     width: CGFloat = 100,
     height: CGFloat = 100,
     startOffset: CGFloat,
     direction: VariableBlurDirection
   ) -> CGImage {
+    if radial {
+      return makeRadialGradientImage(width: width, height: height)
+    }
+
     switch direction {
     case .blurredCenterClearTopAndBottom:
       return makeCenterGradientImage(
@@ -165,6 +311,69 @@
     }
   }
 
+  private func makeMaskCanvasSize() -> CGSize {
+    let width = max(bounds.width, 1)
+    let height = max(bounds.height, 1)
+    let maxSide = max(width, height)
+    let targetMaxSide: CGFloat = 160
+    let scale = targetMaxSide / max(maxSide, 1)
+
+    return CGSize(
+      width: max(ceil(width * scale), 1),
+      height: max(ceil(height * scale), 1)
+    )
+  }
+
+  private func makeRadialGradientImage(
+    width: CGFloat,
+    height: CGFloat
+  ) -> CGImage {
+    guard let context = ensureMaskContext(width: width, height: height),
+          let gradient = ensureRadialGradient() else {
+      return makeFallbackMask(width: width, height: height)
+    }
+
+    let viewWidth = max(bounds.width, 1)
+    let viewHeight = max(bounds.height, 1)
+    let viewMaxSide = max(viewWidth, viewHeight)
+    let maskMaxSide = max(width, height)
+    let scale = maskMaxSide / max(viewMaxSide, 1)
+    let center = CGPoint(
+      x: min(max(radialCenterX, 0), 1) * width,
+      y: min(max(radialCenterY, 0), 1) * height
+    )
+    let clearRadius = currentAnimatedClearRadius * scale
+    let feather = max(currentAnimatedFeather * scale, 1)
+    let opaqueRadius = max(clearRadius + feather, 1)
+    let farthestCorner = max(
+      hypot(center.x, center.y),
+      hypot(width - center.x, center.y),
+      hypot(center.x, height - center.y),
+      hypot(width - center.x, height - center.y)
+    )
+
+    if clearRadius + feather <= 0 {
+      return makeOpaqueMask(width: width, height: height)
+    }
+
+    if clearRadius >= farthestCorner {
+      return makeTransparentMask(width: width, height: height)
+    }
+
+    context.clear(CGRect(x: 0, y: 0, width: width, height: height))
+
+    context.drawRadialGradient(
+      gradient,
+      startCenter: center,
+      startRadius: max(clearRadius, 0),
+      endCenter: center,
+      endRadius: opaqueRadius,
+      options: [.drawsBeforeStartLocation, .drawsAfterEndLocation]
+    )
+
+    return context.makeImage() ?? makeFallbackMask(width: width, height: height)
+  }
+
   private func makeCenterGradientImage(
     width: CGFloat = 100,
     height: CGFloat = 100,
@@ -269,5 +478,373 @@
     context.setFillColor(CGColor(gray: 0, alpha: 1))
     context.fill(CGRect(x: 0, y: 0, width: width, height: height))
     return context.makeImage()!
+  }
+
+  private func makeOpaqueMask(width: CGFloat, height: CGFloat) -> CGImage {
+    return makeSolidMask(width: width, height: height, alpha: 1)
+  }
+
+  private func makeTransparentMask(width: CGFloat, height: CGFloat) -> CGImage {
+    return makeSolidMask(width: width, height: height, alpha: 0)
+  }
+
+  private func makeSolidMask(width: CGFloat, height: CGFloat, alpha: CGFloat) -> CGImage {
+    let colorSpace = CGColorSpaceCreateDeviceRGB()
+    let bitmapInfo = CGImageAlphaInfo.premultipliedLast.rawValue
+
+    guard let context = CGContext(
+      data: nil,
+      width: Int(max(width, 1)),
+      height: Int(max(height, 1)),
+      bitsPerComponent: 8,
+      bytesPerRow: 0,
+      space: colorSpace,
+      bitmapInfo: bitmapInfo
+    ) else {
+      return makeFallbackMask(width: width, height: height)
+    }
+
+    context.setFillColor(UIColor.black.withAlphaComponent(alpha).cgColor)
+    context.fill(CGRect(x: 0, y: 0, width: width, height: height))
+    return context.makeImage() ?? makeFallbackMask(width: width, height: height)
+  }
+
+  private func updateBackdropScaleIfNeeded(_ backdropLayer: CALayer) {
+    guard let window else {
+      return
+    }
+
+    let newScale = window.traitCollection.displayScale
+    let currentScale = backdropLayer.value(forKey: "scale") as? CGFloat
+    if currentScale != newScale {
+      backdropLayer.setValue(newScale, forKey: "scale")
+    }
+  }
+
+  private func interpolate(from: CGFloat, to: CGFloat, progress: CGFloat) -> CGFloat {
+    from + ((to - from) * progress)
+  }
+
+  private func memoriumRadiusEase(_ progress: CGFloat) -> CGFloat {
+    cubicBezierAtTime(progress: progress, p1x: 0.05, p1y: 0.9, p2x: 0.2, p2y: 1)
+  }
+
+  private func applyRadialMaskConfiguration() {
+    ensureStandardBlurConfiguration()
+
+    guard bounds.width > 0, bounds.height > 0 else {
+      layer.mask = nil
+      return
+    }
+
+    let opaqueRadius = max(currentAnimatedClearRadius + currentAnimatedFeather, 0)
+    if opaqueRadius <= 0 {
+      layer.mask = nil
+      return
+    }
+
+    let envelopeRadius = resolvedRadialMaskEnvelopeRadius(
+      clearRadius: currentAnimatedClearRadius,
+      feather: currentAnimatedFeather
+    )
+    configureRadialMaskLayer(envelopeRadius: envelopeRadius)
+
+    CATransaction.begin()
+    CATransaction.setDisableActions(true)
+    radialMaskLayer.colors = radialMaskColors()
+    radialMaskLayer.locations = radialMaskLocations(
+      clearRadius: currentAnimatedClearRadius,
+      feather: currentAnimatedFeather,
+      envelopeRadius: envelopeRadius
+    )
+    CATransaction.commit()
+    layer.mask = radialMaskLayer
+  }
+
+  private func startRadialMaskRevealAnimation(
+    durationMs: CGFloat,
+    delayMs: CGFloat,
+    fromRadius: CGFloat,
+    toRadius: CGFloat,
+    fromFeather: CGFloat,
+    toFeather: CGFloat
+  ) {
+    currentAnimatedClearRadius = fromRadius
+    currentAnimatedFeather = fromFeather
+    ensureStandardBlurConfiguration()
+
+    let envelopeRadius = resolvedRadialMaskEnvelopeRadius(
+      clearRadius: toRadius,
+      feather: toFeather
+    )
+    configureRadialMaskLayer(envelopeRadius: envelopeRadius)
+
+    let startLocations = radialMaskLocations(
+      clearRadius: fromRadius,
+      feather: fromFeather,
+      envelopeRadius: envelopeRadius
+    )
+    let endLocations = radialMaskLocations(
+      clearRadius: toRadius,
+      feather: toFeather,
+      envelopeRadius: envelopeRadius
+    )
+
+    CATransaction.begin()
+    CATransaction.setDisableActions(true)
+    radialMaskLayer.colors = radialMaskColors()
+    radialMaskLayer.locations = endLocations
+    layer.mask = radialMaskLayer
+    CATransaction.commit()
+
+    let revealAnimation = CABasicAnimation(keyPath: "locations")
+    revealAnimation.fromValue = startLocations
+    revealAnimation.toValue = endLocations
+    revealAnimation.beginTime = CACurrentMediaTime() + CFTimeInterval(max(delayMs, 0) / 1000)
+    revealAnimation.duration = CFTimeInterval(max(durationMs, 1) / 1000)
+    revealAnimation.timingFunction = CAMediaTimingFunction(
+      controlPoints: 0.05,
+      0.9,
+      0.2,
+      1
+    )
+    revealAnimation.fillMode = .both
+    revealAnimation.isRemovedOnCompletion = true
+    radialMaskLayer.add(revealAnimation, forKey: "radialRevealLocations")
+
+    radialClearRadius = toRadius
+    radialFeather = toFeather
+    currentAnimatedClearRadius = toRadius
+    currentAnimatedFeather = toFeather
+  }
+
+  private func ensureBaseConfiguration() -> NSObject? {
+    if effect == nil || appliedBlurStyle != currentBlurStyle {
+      effect = UIBlurEffect(style: currentBlurStyle)
+      appliedBlurStyle = currentBlurStyle
+      backdropLayerRef = nil
+    }
+
+    guard let variableBlur = ensureVariableBlurFilter(),
+          let backdropLayer = ensureBackdropLayer() else {
+      return nil
+    }
+
+    if let filters = backdropLayer.filters as? [NSObject], filters.first !== variableBlur {
+      backdropLayer.filters = [variableBlur]
+    } else if backdropLayer.filters == nil {
+      backdropLayer.filters = [variableBlur]
+    }
+
+    updateBackdropScaleIfNeeded(backdropLayer)
+
+    for subview in subviews.dropFirst() {
+      if subview.alpha != 0 {
+        subview.alpha = 0
+      }
+    }
+
+    return variableBlur
+  }
+
+  private func ensureStandardBlurConfiguration() {
+    if effect == nil || appliedBlurStyle != currentBlurStyle {
+      effect = UIBlurEffect(style: currentBlurStyle)
+      appliedBlurStyle = currentBlurStyle
+      backdropLayerRef = nil
+    }
+
+    if let backdropLayer = ensureBackdropLayer() {
+      if let filters = backdropLayer.filters as? [NSObject],
+         let variableBlurFilter,
+         filters.contains(where: { $0 === variableBlurFilter }) {
+        backdropLayer.filters = nil
+      }
+
+      updateBackdropScaleIfNeeded(backdropLayer)
+    }
+
+    for subview in subviews.dropFirst() {
+      if subview.alpha != 0 {
+        subview.alpha = 0
+      }
+    }
+  }
+
+  private func configureRadialMaskLayer(envelopeRadius: CGFloat) {
+    let safeEnvelopeRadius = max(envelopeRadius, 1)
+    let diameter = safeEnvelopeRadius * 2
+    let originPoint = CGPoint(
+      x: min(max(radialCenterX, 0), 1) * bounds.width,
+      y: min(max(radialCenterY, 0), 1) * bounds.height
+    )
+
+    CATransaction.begin()
+    CATransaction.setDisableActions(true)
+    radialMaskLayer.type = .radial
+    radialMaskLayer.startPoint = CGPoint(x: 0.5, y: 0.5)
+    radialMaskLayer.endPoint = CGPoint(x: 1, y: 0.5)
+    radialMaskLayer.bounds = CGRect(x: 0, y: 0, width: diameter, height: diameter)
+    radialMaskLayer.position = originPoint
+    CATransaction.commit()
+  }
+
+  private func radialMaskColors() -> [CGColor] {
+    [
+      UIColor.clear.cgColor,
+      UIColor.clear.cgColor,
+      UIColor.black.cgColor
+    ]
+  }
+
+  private func radialMaskLocations(
+    clearRadius: CGFloat,
+    feather: CGFloat,
+    envelopeRadius: CGFloat
+  ) -> [NSNumber] {
+    let safeEnvelopeRadius = max(envelopeRadius, 1)
+    let safeClearRadius = max(clearRadius, 0)
+    let safeFeather = max(feather, 1)
+    let clearStop = min(max(safeClearRadius / safeEnvelopeRadius, 0), 1)
+    let opaqueStop = min(max((safeClearRadius + safeFeather) / safeEnvelopeRadius, clearStop), 1)
+
+    return [
+      0,
+      NSNumber(value: Double(clearStop)),
+      NSNumber(value: Double(opaqueStop))
+    ]
+  }
+
+  private func resolvedRadialMaskEnvelopeRadius(clearRadius: CGFloat, feather: CGFloat) -> CGFloat {
+    let farthestCorner = max(
+      hypot(bounds.width * radialCenterX, bounds.height * radialCenterY),
+      hypot(bounds.width - (bounds.width * radialCenterX), bounds.height * radialCenterY),
+      hypot(bounds.width * radialCenterX, bounds.height - (bounds.height * radialCenterY)),
+      hypot(bounds.width - (bounds.width * radialCenterX), bounds.height - (bounds.height * radialCenterY))
+    )
+
+    return max(clearRadius + feather, farthestCorner + max(feather, 1), 1)
+  }
+
+  private func updateFilterMaskAndRadius(_ variableBlur: NSObject, forceMaskRefresh: Bool) {
+    if lastAppliedBlurRadius != maxBlurRadius {
+      variableBlur.setValue(maxBlurRadius, forKey: "inputRadius")
+      lastAppliedBlurRadius = maxBlurRadius
+    }
+
+    let shouldRefreshMask = forceMaskRefresh
+      || lastRenderedClearRadius != currentAnimatedClearRadius
+      || lastRenderedFeather != currentAnimatedFeather
+
+    guard shouldRefreshMask else {
+      return
+    }
+
+    let maskCanvasSize = makeMaskCanvasSize()
+    let gradientImage = makeGradientImage(
+      width: maskCanvasSize.width,
+      height: maskCanvasSize.height,
+      startOffset: startOffset,
+      direction: direction
+    )
+
+    variableBlur.setValue(gradientImage, forKey: "inputMaskImage")
+    variableBlur.setValue(true, forKey: "inputNormalizeEdges")
+    lastRenderedClearRadius = currentAnimatedClearRadius
+    lastRenderedFeather = currentAnimatedFeather
+  }
+
+  private func ensureMaskContext(width: CGFloat, height: CGFloat) -> CGContext? {
+    let targetSize = CGSize(width: width, height: height)
+    if let cachedMaskContext, cachedMaskCanvasSize == targetSize {
+      return cachedMaskContext
+    }
+
+    let colorSpace = CGColorSpaceCreateDeviceRGB()
+    let bitmapInfo = CGImageAlphaInfo.premultipliedLast.rawValue
+    let context = CGContext(
+      data: nil,
+      width: Int(max(width, 1)),
+      height: Int(max(height, 1)),
+      bitsPerComponent: 8,
+      bytesPerRow: 0,
+      space: colorSpace,
+      bitmapInfo: bitmapInfo
+    )
+
+    cachedMaskContext = context
+    cachedMaskCanvasSize = targetSize
+    return context
+  }
+
+  private func ensureRadialGradient() -> CGGradient? {
+    if let cachedRadialGradient {
+      return cachedRadialGradient
+    }
+
+    let colorSpace = CGColorSpaceCreateDeviceRGB()
+    let colors = [
+      UIColor.clear.cgColor,
+      UIColor.black.cgColor
+    ] as CFArray
+    let locations: [CGFloat] = [0, 1]
+    let gradient = CGGradient(
+      colorsSpace: colorSpace,
+      colors: colors,
+      locations: locations
+    )
+    cachedRadialGradient = gradient
+    return gradient
+  }
+
+  private func invalidateMaskResources() {
+    cachedMaskContext = nil
+    cachedMaskCanvasSize = .zero
+    lastRenderedClearRadius = nil
+    lastRenderedFeather = nil
+  }
+
+  private func cubicBezierAtTime(
+    progress: CGFloat,
+    p1x: CGFloat,
+    p1y: CGFloat,
+    p2x: CGFloat,
+    p2y: CGFloat
+  ) -> CGFloat {
+    let cx = 3 * p1x
+    let bx = 3 * (p2x - p1x) - cx
+    let ax = 1 - cx - bx
+    let cy = 3 * p1y
+    let by = 3 * (p2y - p1y) - cy
+    let ay = 1 - cy - by
+
+    func sampleCurveX(_ t: CGFloat) -> CGFloat {
+      ((ax * t + bx) * t + cx) * t
+    }
+
+    func sampleCurveY(_ t: CGFloat) -> CGFloat {
+      ((ay * t + by) * t + cy) * t
+    }
+
+    func sampleCurveDerivativeX(_ t: CGFloat) -> CGFloat {
+      (3 * ax * t + 2 * bx) * t + cx
+    }
+
+    var t = min(max(progress, 0), 1)
+    for _ in 0..<8 {
+      let currentSlope = sampleCurveDerivativeX(t)
+      if abs(currentSlope) < 0.000001 {
+        break
+      }
+
+      let currentX = sampleCurveX(t) - progress
+      t -= currentX / currentSlope
+    }
+
+    return sampleCurveY(min(max(t, 0), 1))
+  }
+
+  deinit {
+    stopRevealAnimation()
   }
 }
diff -ruN a/node_modules/@sbaiahmed1/react-native-blur/src/ReactNativeProgressiveBlurViewNativeComponent.ts b/node_modules/@sbaiahmed1/react-native-blur/src/ReactNativeProgressiveBlurViewNativeComponent.ts
--- a/node_modules/@sbaiahmed1/react-native-blur/src/ReactNativeProgressiveBlurViewNativeComponent.ts	2026-03-06 18:10:31
+++ b/node_modules/@sbaiahmed1/react-native-blur/src/ReactNativeProgressiveBlurViewNativeComponent.ts	2026-03-06 18:10:31
@@ -38,6 +38,18 @@
   blurType?: WithDefault<BlurType, 'regular'>;
   direction?: WithDefault<ProgressiveBlurDirection, 'blurredTopClearBottom'>;
   startOffset?: WithDefault<Double, 0.0>;
+  radial?: WithDefault<boolean, false>;
+  radialCenterX?: WithDefault<Double, 0.5>;
+  radialCenterY?: WithDefault<Double, 0.5>;
+  radialClearRadius?: WithDefault<Double, 0.0>;
+  radialFeather?: WithDefault<Double, 120.0>;
+  revealTrigger?: WithDefault<Double, 0.0>;
+  animationDuration?: WithDefault<Double, 20000.0>;
+  animationDelay?: WithDefault<Double, 200.0>;
+  startRadius?: WithDefault<Double, 0.0>;
+  endRadius?: WithDefault<Double, 1000.0>;
+  featherStart?: WithDefault<Double, 120.0>;
+  featherEnd?: WithDefault<Double, 160.0>;
   reducedTransparencyFallbackColor?: WithDefault<string, '#FFFFFF'>;
 }
 
